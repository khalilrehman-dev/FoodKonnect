package com.khalil.foodkonnect.ui.onboarding

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.khalil.foodkonnect.R

// Placeholder for now — full onboarding carousel + role selection built next.
class OnboardingActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_splash) // temporary reuse, replaced when real layout exists
    }
}